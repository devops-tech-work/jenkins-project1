pipeline {
    agent none
     stages{
        stage("build"){
            steps{
                echo "stage build"
            }
        }
        stage("Test"){
            steps {
                echo "stage test"
            }
        }
     }
}